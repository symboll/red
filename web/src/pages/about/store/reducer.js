const defaultState = {

}

const reducer = (state = defaultState, action) => {
  switch (action.type) {
    case '':
      break;

    default: console.log('xx')
  }
  return state
}

export default reducer